<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NewHabitActivity">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/create_fragment_container_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="10dp"
        android:name="com.example.sestudentjuhabitapp.EditHabitFragment"
        app:layout_constraintBottom_toTopOf="@id/create_create_habit_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:name="com.example.sestudentjuhabitapp.EditHabitFragment"/>
    <!-- android:name="fragment_name" does not work when the creation method takes in arguments, don't yet know if mine will -->

    <!-- constrained the fuck out of it, stop complaining now android studio :( -->
    <Button
        android:id="@+id/create_create_habit_button"
        android:layout_margin="10dp"
        android:layout_width="268dp"
        android:layout_height="74dp"
        android:text="@string/create_create_habit_button_text"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>


</androidx.constraintlayout.widget.ConstraintLayout>